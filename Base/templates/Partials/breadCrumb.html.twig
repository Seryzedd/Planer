{% if content is defined %}
    <nav aria-label="breadcrumb" class="fit-content">
        <ol class="breadcrumb">
            {% for name, link in content %}
                <li class="breadcrumb-item" {% if loop.index == content|length %}aria-current="{{ name }}"{% endif %}>
                    
                    {% if loop.index == content|length %}
                        <span class="text-muted mb-0">
                            {% if loop.first %}
                                <i class="fa-regular fa-house"></i>
                            {% endif %}

                            
                            {% if '%' in name and link|isTypeOf('string') == false %}
                                {{ name|trans(link.params, 'messages') }}
                            {% else %}
                                {{ name|trans|capitalize }}
                            {% endif %}
                        </span>
                    {% else %}
                        {% if link|isTypeOf('string') %}
                            <a href="{{ path(link)}}" class="text-primary">
                                {% if loop.first %}
                                    <i class="fa-solid fa-house"></i>
                                {% endif %}
                                
                                {% if '%' in name and link|isTypeOf('string') == false %}
                                    {{ name|trans(link.params, 'messages') }}
                                {% else %}
                                    {{ name|trans|capitalize }}
                                {% endif %}
                            </a>
                        {% else %}
                            <a href="{{ path(link.name, link.params)}}" class="text-primary">
                                {% if loop.first %}
                                    <i class="fa-solid fa-house"></i>
                                {% endif %}
                                
                                {% if '%' in name and link|isTypeOf('string') == false %}
                                    {{ name|trans(link.params, 'messages') }}
                                {% else %}
                                    {{ name|trans|capitalize }}
                                {% endif %}
                            </a>
                        {% endif %}
                        
                    {% endif %}
                </li>
            {% endfor %}
        </ol>
    </nav>
{% endif %}