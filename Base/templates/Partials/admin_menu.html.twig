
<div id="admin-menu" class="d-flex align-items-center justify-items-stretch">
    <div class="sticky-top">
        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title='Admin index' href="{{ path('admin_index')}}">
            <i class="fas fa-tachometer-alt"></i>
        </a>

        {% if is_granted('ROLE_COMPANY_LEADER') %}
            <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title='Company' href="{{ path('admin_company_index')}}">
                <i class="far fa-building"></i>
            </a>
        {% endif %}
        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title='Teams' href="{{ path('admin_teams')}}">
            <i class="fas fa-user-plus"></i>
        </a>
        
        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title='Users' href="{{ path('admin_users')}}">
            <i class="fas fa-users-cog"></i>
        </a>

        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title="Absences" href="{{ path('admin_absences_index') }}">
            <i class="fas fa-plane"></i>
        </a>

        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title="Invitations" href="{{ path('admin_invitations')}}">
            <i class="fas fa-envelope-open-text"></i>
        </a>

        

        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title='Projects'  href="{{ path('admin_projects')}}">
            <i class="fas fa-project-diagram"></i>
        </a>
        
        <a class="btn btn-dark text-secondary rounded-0 mt-0 w-100" title="Clients" href="{{ path('admin_clients')}}">
            <i class="fas fa-mug-hot"></i>
        </a>
    </div>
</div>
