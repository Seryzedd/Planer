
# Créer le conteneur portainer
docker volume create portainer

# Lancer le projet
docker compose up

# Nouveau mot de passe
docker run --rm -v portainer_data:/data portainer/helper-reset-password

# container terminal
docker exec -it Planer bash
docker compose exec web bash
